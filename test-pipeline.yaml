kind : "BuildConfig"

apiVersion: "v1"
metadata:
  name: "frontend-builder"
spec:
  source:
    git:
      uri: "https://github.com/DerBrecher/frontend-pipeline"
  strategy:
    jenkinsPipelineStrategy:
      jenkinsfilePath: Jenkinsfile
    type: JenkinsPipeline